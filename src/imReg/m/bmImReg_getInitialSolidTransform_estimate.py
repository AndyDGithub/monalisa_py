from __future__ import annotations
import numpy as np

def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    mySolidTransform = None
    return mySolidTransform


def bmImReg_getInitialSolidTransform_estimate(imRef, imMov, X, Y, Z):
    """Estimate the initial solid transformation between two images."""
    #
    # Bastien Milani
    # CHUV and UNIL
    # Lausanne - Switzerland
    # May 2023
    #
    my
