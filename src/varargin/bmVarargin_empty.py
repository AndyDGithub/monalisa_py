import numpy as np
from src.arrayUtility import bmBlockReshape

def bmVarargin_empty():
    varargout = [np.array([]) for _ in range(0)]  # Initializing an empty list with zero elements

    return varargout
